// ⚠️ Preencha com as chaves do SEU projeto Firebase.
// Estas chaves (apiKey, projectId etc.) são públicas por design — o Firebase
// não trata isso como segredo, então é seguro deixá-las neste arquivo.
// A CHAVE QUE É SEGREDO é a "conta de serviço" (service account JSON),
// usada em functions/send-notification.js — essa NUNCA vai aqui nem no chat.

const firebaseConfig = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: "",
  // Gerado em: Firebase Console > Project Settings > Cloud Messaging > Web Push certificates
  vapidKey: "",
};
